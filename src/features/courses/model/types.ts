export type CourseListElement = {
  id: string;
  name: string;
  description: string;
};
export type CreateCourse = {
  name: string;
  description: string;
};
export type DeleteCourse = {
  id: string;
};
